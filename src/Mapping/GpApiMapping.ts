import { Transaction } from "src/Entities";

export class GpApiMapping {
  public static mapResponseAPM(response: string) {
    response;
  }

  public static mapResponse(response: string): Transaction {
    response;
    return {} as Transaction;
  }
}
